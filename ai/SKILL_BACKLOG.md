# Skill Ideas Backlog

Log of opportunities to create new skills, improve existing ones, or split / merge skills. Triage periodically — promote to action, or move an entry down to Archived once it ships.

**Where new entries go:** the bottom of `## Active`, never the bottom of the file. Appending blind to the end of the file files a brand-new idea under Archived, where triage never looks — that silently swallowed ten entries between 2026-06-11 and 2026-08-12.

**Format:** `- YYYY-MM-DD — short description (type: new | improve | split | merge). Why this matters.`

Keep entries to a single line where possible. Link to relevant context (X posts, conversations, files) inline. Entries in Active are sorted oldest-first. When archiving, add a one-line reason if it is anything other than "shipped" — superseded, moot, or abandoned all read the same from a distance otherwise.

## Active

- 2026-04-27 — Audit all existing skills under `~/dotfiles/ai/skills/` for an explicit `## Out of Scope` section (type: improve). Per [Anatomy of a Perfect Skill](https://x.com/zodchiii/status/2048345453096313005) pattern 5 — most skills lack this and it's the highest-impact addition. Pattern: explicitly list what the skill does NOT do.
- 2026-04-27 — Audit all existing skills against the six Anatomy patterns broadly (type: improve). Especially patterns 2 (directive imperative language), 3 (explicit output format), and 4 ("read first" step). Reference: writing-skills SKILL.md → "Six Anatomy Patterns" section.
- 2026-04-28 — Audit `weekly-update-formatter` and `message-crafter` for the same fabrication loophole as writing-pr-descriptions (type: improve). Why: confident-voice constraints + AI agent producing artifacts about events the AI didn't witness = high risk of plausibly-wrong past-tense claims. Apply the same fix pattern: require unchecked checklists / "haven't verified" disclaimers when the writer didn't observe the event firsthand.
- 2026-05-18 — Add a `humanizer` skill (type: new). Why: even after invoking writing-pr-descriptions, AI-authored PR bodies leak tells — parallel construction, balanced sentence rhythm, "Reasoning:"-style preambles, every paragraph landing on a complete thought, etc. A focused post-pass skill should diff the draft against Sean's voice (SOUL.md + PR Description Guidelines) and call out specific AI-tells with proposed edits. Should also work on Slack messages, Linear comments, and other async written outputs — not just PRs. Triggered by phrases like "humanize this", "make this sound human", "AI-tell check", or applied automatically as a final pass on writing-pr-descriptions / message-crafter / weekly-update-formatter outputs.
- 2026-05-27 — Add a `context-handoff` skill (type: new). Why: when Sean wants to fork a long session into a focused fresh one (e.g., "build the Hex dashboard" + "write the rollout plan doc" out of a multi-day SbD thread), the manual move is to distill the relevant subset of context into a paste-able first-message for the new session. Today we hand-rolled this; the pattern is structured enough to encode: identify the deliverables, extract supporting facts (IDs, file paths, URLs, fresh numbers), name what's out of scope, and end with "where would you suggest I start?" to convert the new session into a planning conversation immediately. Trigger phrases: "fork this conversation", "continue this with less context", "start a focused session", "handoff to a new thread". Better than `/compact` because it's lossless authorship of the next session's starting state vs lossy summarization in-place.
- 2026-05-27 — Add a `verify-metric-definition` skill (type: new). Why: today we walked the entire dbt dependency chain for `rest_activated_at` to confirm whether failed PostgREST requests count toward activation — answer required tracing 6 models across two parallel paths and grepping every `status`-related reference to distinguish HTTP status from project lifecycle status. This pattern (given a dbt metric, prove what it does and doesn't filter on) recurs whenever metric semantics are questioned (Hex readouts, experiment guardrails, finance reports). Skill should: locate the metric definition, walk `ref()` calls upstream, audit every WHERE/JOIN at each layer, and identify columns dropped vs preserved at each aggregate level. Output is a chain map + filter audit + "what would it take to add X" estimate.
- 2026-05-27 — Add a `writing-cross-functional-decision-docs` skill (type: new). Why: today we co-authored a phased rollout plan for the `dataApiRevokeOnCreateDefault` flag (Notion: "Revoke Default Grants Rollout Plan", id `36d5004b-775f-819d-b0f5-ecbd7d630ca2`; local draft: `~/supabase/docs/plans/2026-05-27-data-api-revoke-on-create-rollout-plan.md`). Started bloated (Notion AI: "reads like an analytics plan, not a rollout plan"), ended sharp after ~15 rounds. Generalizable principles to encode: (1) **Status callout at top** answers "where are we right now? what is this review asking for?" before anything else — burying current state in the Decision Log is wrong. (2) **Overview leads with customer impact**, not feature mechanic ("New projects no longer grant Data API privileges to anon/authenticated by default" beats "the flag flips the Grant default privileges checkbox"). (3) **Strawman thresholds + visual highlight + explicit "push back" framing** — vastly higher reviewer engagement than `TODO%` placeholders. Use `<span color="yellow_bg">` in Notion. Pair with a sentence like "these highlighted thresholds are a strawman — push back on the numbers themselves." (4) **Table altitude matches decision altitude.** 5-row phase table = strategy reviewers approve; 25-row step table = tracker for execution. Don't conflate; pick the right granularity for the doc's job, and offload tracking to a separate artifact (Decision Log, Linear, dashboard). (5) **Strict sequencing over hedge clauses** — no "Phase X complete OR trips diagnosed/remediated" escape hatches. Commit, amend later via Decision Log. Pre-written escape hatches signal you're already negotiating against your own plan. (6) **Cohort-anchored ramp descriptions** when progression is per-customer ("small SFP → larger SFP") not per-percentage — don't shoehorn into percentages just because tables look uniform. (7) **`<details>` toggles preserve analytical depth without bloating linear read** — great for diagnostic interpretation rules, known limitations, edge-case nuance, and anything that matters to one reviewer but distracts others. (8) **Footnotes with formula + worked example + justification** (not just definition). Justification preempts the "shouldn't this be different?" reviewer question. Example pattern: "Formula: `(control − test) / control`. Example: 2% relative regression on 80% baseline = test dropped to ~78.4%. Used for: rate-style metrics; scales fairly across cohorts." (9) **Pass-criterion framing**: relative regression for rate-style metrics (scales across cohorts with different baselines), absolute rise for near-zero metrics (avoids divide-by-near-zero distortion where 0.1→1.0 reads as 900% relative but is only 0.9pp absolute). (10) **Iterative deletion IS the work.** Final draft was ~50% shorter than first. The signal you're done editing is when each round produces deletions rather than additions. When a section's purpose has been absorbed by another section (e.g., metric definitions migrating from prose into a Guardrails table), delete the section cleanly rather than try to preserve every paragraph. (11) **Anti-pattern: bolded-title-then-explanation bullets** create a false sentence break and read unnaturally. Plain prose paragraphs or definition-style "**Field:** value" labels both work; mid-bullet sentence splits ("**Title.** explanation sentence") don't. (12) **Filter LLM doc feedback critically.** "Restructure into PRD template" is the default over-correction when only 1-2 surgical edits address the real issues. When LLM feedback says "restructure," ask which paragraphs specifically would move; if the answer is "all of it," the feedback is reaching for a template. Skill should encode these as a checklist + a worked before/after pair showing how a too-technical analytics-style draft becomes a stakeholder-ready rollout plan. Closely related to but distinct from `writing-pr-descriptions` (different artifact, similar voice discipline) and `writing-plans` (similar artifact, but typically internal-only and lower-stakes than cross-functional).
- 2026-06-11 — Prune trigger overlap between Matt Pocock's installed skills and existing personal skills (type: merge). Installed 17 skills from mattpocock/skills via skills.sh (symlinked ~/.claude/skills/ → ~/.agents/skills/). Competing pairs with near-identical triggers: `tdd` vs `test-driven-development`, `diagnose` vs `systematic-debugging`, `write-a-skill` vs `writing-skills`, `triage` vs inbox-triage/slack-mention-triage. Which fires on a matching prompt is effectively random. After trialing Matt's versions, pick a winner per pair — keep one, remove or de-trigger the other (his are read-only symlinks; edits mean forking into ~/dotfiles/ai/skills/).
- 2026-06-11 — message-crafter: consult memory/PEOPLE.md (register map + per-person positioning notes) when drafting messages to known teammates (type: improve). PEOPLE.md now holds an explicit tone gradient (casual Pam/Aleksi → creative Saxon → solutions+data Marc → deferential Inian); skill currently guesses register per recipient.
- 2026-06-11 — subagent-driven-development: add explicit reviewer instruction to verify implementer-claimed precedents/rationales for skipped or deviated work (type: improve). Two fabricated-rationale cases caught in one session (kizuna surveys build) only because reviewers checked the cited precedent against the repo.
- 2026-06-17 — email-broadcast-build: Stage 10 prod-send/schedule mechanism is stale (type: improve). Skill says POST `/newsletters/{id}/forward` with `{newsletter:{scheduled_for}}`. In the live CIO API (workspace 187397, GROWTH-919) `/forward` is "forward to one customer" — it requires `{customer_id, internal_id, template_id}` and is NOT the broadcast trigger. Correct mechanism is PUT `/newsletters/{id}` with `update_type:"schedule"` (`scheduled_at` unix-seconds + `timezone` IANA + `tz_match_enabled` bool) for scheduled sends, or `update_type:"send"` (no extra fields) for immediate. Verified: scheduling lands `scheduled_at` on the newsletter and leaves `sent_at:null` until fire. Update Stage 10 to use the schedule/send update_types and drop the `/forward` recipe.
- 2026-06-17 — email-broadcast-build: document the workspace "Allow agent to edit live data" gate (type: improve). With it OFF, agent-driven send/schedule returns `403` ("…blocking agents from sending, scheduling, or managing live sends… enable at https://fly.customer.io/settings/ai") even though build steps (segment/import/newsletter/template) all succeed. The block is atomic (no partial state) and read calls still work. Skill should: (a) preflight-check or at least warn about this setting before Stage 10, (b) note it needs a workspace admin, (c) note re-auth may be needed for the agent session to pick up the change, and (d) document the UI fallback (open newsletter → Schedule) as the zero-dependency path. Also: token scope already includes `write:live`, so the gate is purely the workspace setting, not OAuth scope.
- 2026-06-17 — email-broadcast-build: CIO MCP tool interface is split by verb, not a single `cio_api` (type: improve). Live tools are `cio_read_api` (GET), `cio_write_api` (POST/PUT/PATCH), `cio_delete_api` (DELETE), plus `cio_schema`/`cio_prime`/`cio_skills_*`. Each takes `path`+`params`+`body`(object)+`dry_run`+`jq`. Skill's `mcp__customerio__cio_api({...read_only...})` calls no longer match. Update all stage examples to the verb-split tools and the `dry_run`/`jq` flags; note `cio_prime` (API conventions) and `cio_skills_read fly-api/...` should be consulted first.
- 2026-07-14 — writing-skills / writing-great-skills: add two empirically-tested authoring rules discovered while TDD-ing the writing-as-sean skill (type: improve). (1) COUNTER-EXAMPLES TEACH THE BEHAVIOR THEY FORBID. A rule reading "write X, never Y" reliably produced Y. The em-dash rule failed 3 consecutive runs while the skill file contained a literal em-dash in its own "never write this" example; deleting the character from the file fixed compliance immediately, with no change to the rule's wording. Corollary: never put a banned token, phrase, or construction literally in a skill file, even inside a Don't column. Describe it or omit it. This is Pocock's negation principle, confirmed under test. (2) SEARCH RULES PASS, JUDGMENT RULES FAIL. "Don't narrate the investigation" failed 4 runs; the identical intent rewritten as "search your draft for 'I found, I traced, in one run, confirmed' and delete those whole sentences" passed first try. Any rule the agent can rationalize past ("this evidence IS load-bearing") should be restated as a literal string search with a zero count. (3) Placement matters more than wording: instructions in a numbered "Hard rules" block near the top were obeyed; the same instructions in a later "Self-check" section were skipped. **Status 2026-08-12:** rule (1) landed as the Negation bullet in `writing-great-skills` (line 83); rules (2) search-rules-beat-judgment-rules and (3) placement-in-Hard-rules did NOT land.
- 2026-07-15 — writing-as-sean: two refinements found while drafting a 1:1 vendor note (type: improve). (1) SUPERLATIVE-CROWNING IS ANNOUNCE-BEFORE-SAYING. "The session traces are the standout: [thing]", "the key thing is [thing]", "the big win is [thing]" are the same tic Rule 10 already forbids for lists/questions — a label promising the content before delivering it. Just state the thing. Extend Rule 6/10 with a literal search for crowning openers: `the standout|the key thing|the big win|the best part|what's notable|the highlight`. (2) REGISTER EXCEPTION FOR RELATIONSHIP 1:1s. The "warmth is for the author, cut it" stance is calibrated for internal team writing and misfires in a 1:1 to an external partner doing us a favor (early-access beta, free credits). There, a genuine appreciation line ("we've been playing with the demo and already found some really useful things") is load-bearing relationship maintenance, not filler — and Sean's own voice there IS lightly warm, including an enthusiasm word the current grep would wrongly flag. Add a register carve-out: external-favor 1:1s get one genuine warm line; the enthusiasm-word/warmth greps should treat it as allowed context, not a violation. Evidence: the Gauge feedback+credits note, this session.
- 2026-07-21 — writing-as-sean critic loop needs a no-response fallback (type: improve). The skill mandates dispatching writing-critic as a subagent and looping until CLEAN, but in one session 3 of 6 dispatched subagents went idle without ever sending a verdict. Skill should say: ping once, and if still silent, fall back to a deterministic grep of the countable rules and tell Sean the judgment pass was not independent.
- 2026-07-24 — collaborative-draft-in-markdown: draft shared prose (updates, PRs, docs) in a scratchpad .md file opened in the editor, iterate via direct edits + "pick up my changes" instead of copy/paste in chat (type: new). Why: passing draft context back and forth in chat is lossy and slow; a live file the user edits in Zed keeps one source of truth and pairs naturally with the writing-as-sean critic loop.
- 2026-07-27 — work-sweep Agent 4 (GitHub): `review-requested:<user>` misses team/CODEOWNERS review requests (type: improve). Why: supabase#47979 (scoped PAT form refactor, +3351/-337, stacked, updated same day) was invisible to the sweep and only surfaced because GitHub emailed Sean about it — the Gmail agent found what the GitHub agent structurally could not. GitHub's `review-requested:` qualifier matches only *individual* requests; requests routed through a team or CODEOWNERS need `team-review-requested:<org/team>`. Fix: add a second query for team requests (Growth Eng + any team Sean belongs to), and dedupe against the individual-request results. Ironic detail worth keeping in the fix note: the existing prompt already has elaborate logic for *excluding* CODEOWNERS blanket requests, so it assumes they show up — but the query it uses never returns them in the first place.
- 2026-07-27 — work-sweep Agent 1 (Slack) under-detects ball-in-court when the ask lives in a thread REPLY or an external/Connect channel (type: improve). Why: Leon (PostHog) asked Sean and Alaister a direct question — "have you guys had a chance to set up session replay yet?" — at 00:51 PDT the morning of the sweep, in an unanswered 4-day-old thread. The sweep ran ~10 hours later and did not surface it; Sean found it himself and asked for a task. The agent returned only 2 ball-in-court items total for the whole workspace, which in hindsight was the tell. Root cause NOT confirmed (no agent transcript retained) — plausible candidates, in rough order: (a) the mention is in a thread reply, not a parent message, and the search queries may only rank/return parents; (b) the prompt enumerates specific channel patterns to sweep (#team-growth-eng, "#project-*, #partner-*") which biases coverage away from external/Slack-Connect vendor channels — this one was C07P0E4R3MW, a PostHog channel matching none of those; (c) `to:@sean` / `mentions:@sean` may not resolve reliably against the enterprise workspace. Fix regardless of which: drop the channel-name allowlist in favor of recency+participation, explicitly instruct the agent to search thread replies (`is:thread`) for unanswered questions directed at Sean, and add a sanity self-check — if ball-in-court comes back with fewer than ~3 items across a full workspace, say so as low-confidence rather than reporting it as a clean sweep. Related: this is the third coverage gap found in one sweep (see the two 2026-07-27 entries above re: GitHub team-review-requested and Gmail OAuth retry) — the pattern is that each per-source agent fails SILENTLY and the synthesis presents a partial sweep as a complete one, so consider a cross-cutting fix: every agent must report what it could NOT cover, and Phase 2 must surface those admissions in the report body.
- 2026-07-27 — work-sweep needs a stale-snapshot guard before Phase 5 writes tasks (type: improve). Why: the Linear agents queried at 17:29Z and saw GROWTH-984/985/987 as In Progress + unassigned; Sean did the ownership pass at 17:31–17:43Z in a parallel session. By the time Phase 5 ran, a created Things task ("put owners on 984/985/987") was already obsolete — 984 was Done and all three assigned. Caught only because the sessions.md entry contradicted the report and prompted an API check. Fix: before creating or scheduling any task derived from a Linear/GitHub item, re-fetch that item's current status/assignee from the API and skip or auto-complete it if resolved. Cheap (a handful of get_issue calls), and it's the difference between a plan and a plan that's already wrong. Generalizes: a fan-out sweep's output has a shelf life measured in minutes, so any write-back phase must re-verify, never trust the report.
- 2026-08-11 — Create a persistent "adhd-mode" output style, adapted from github.com/ayghri/i-have-adhd (type: new). Why: comparing it against `/eli5` (one-shot, re-explains only the last message) surfaced rules Cortex's SOUL.md gestures at but never operationalizes: numbered steps for multi-step work, restating "step N of M" every turn so state survives across messages, concrete time estimates instead of vague ones, and a pre-send checklist (strip announcing openers, hedge words, idioms) before sending. Unlike eli5, it would be sticky for the rest of a session (on via trigger phrase, off via "stop adhd mode") rather than scoped to one message — keep eli5 as-is for the single-message-translate case, don't merge them.
- 2026-08-12 — shape-today: add a capped `🚧 Unblocks` carve-out that sits outside the 3-substantive-task limit (type: improve). Sean's ask, design agreed in conversation. The trigger case: "ask Caelean to fix the Order Date and restore the opt-out" is a two-minute Slack message that gates a legal review, a redline, and a vendor decision — making it compete with "write the ALG findings doc" for one of three slots is obviously wrong, but exempting it needs guardrails or it rebuilds the 28-item Today the cap exists to prevent. Design: (1) the criterion is BLOCKING, not size — "something or someone else is waiting on this" — because size is a bad proxy and self-estimated size is biased downward on exactly the small tasks used to avoid the hard one; (2) hard cap of 3, counted and displayed on a separate line above the main three, never hidden, since an uncapped exemption is just a second Today list; (3) batched into one block and done first, before substantive work; (4) the tag mirrors the existing `🟡 Waiting` ("I'm blocked on someone") as its inverse ("someone is blocked on me"), so the vocabulary is already established. Rejected `⚡️ 2min` / size framing for the reasons in (1). Also note the real cost of a "two-minute" task is the context switch into Slack, not the typing, which is a second argument for batching rather than interleaving. Needs a matching one-line addition to `add-todo`'s tag table (currently exactly three tags, and that constraint is load-bearing — this makes four).
- 2026-08-12 — The anti-fabrication guardrail REGRESSED out of `writing-pr-descriptions`; audit and restore it across all three confident-voice skills (type: improve). Verified today: the 2026-04-28 entry (below, in Archived, marked "Shipped same day") says principle #8, the Testing template, Common Mistakes, and Red Flags were patched to require unchecked `- [ ]` checklists for unverified items and to forbid fabricated past-tense. None of that is in the current file. It was restructured into "Step 1–8" on 2026-07-14 and now has no Red Flags section, no Common Mistakes section, and no honesty/anti-invention language anywhere — greps for `invent|make up|didn't test|honest|verif` find only incidental hits. Plausible innocent cause: the 2026-07-14 negation rule ("never put a banned token literally in a skill file, even in a Don't column") would have stripped the literal `- [ ]` examples, and the surrounding rule may have gone with them. Two things to fix, and the second is the bigger one: (1) restore the guardrail in all three skills — `writing-pr-descriptions` (regressed), plus `weekly-update-formatter` and `message-crafter`, which the 2026-04-28 follow-up flagged as probably sharing the loophole and which were never audited; (2) nothing detected that a shipped, GREEN-verified rule vanished in a later rewrite of the same file, which is a process gap, not a one-off — consider a cheap guard, e.g. skills record their load-bearing rules as a short checklist the writing-skills flow re-checks after any rewrite. Highest-consequence class of skill bug we have: these three skills all write in Sean's confident voice about events the model did not witness.

## Archived

_(items completed, superseded, or no longer relevant)_

- 2026-05-19 — `slack-mention-triage` skill (type: new). **Shipped same day.** Required output is summary → verdict → one next action → optional handoff to message-crafter. RED baseline came from the 2026-05-19 Scope/AEO triage where the first-pass response skipped the summary on the implicit rationalization "Sean shared the URL so he knows what's in it" — Sean explicitly corrected this. Skill encodes "summary always, regardless of verdict" as the core insight.
- 2026-04-28 — `writing-pr-descriptions` should require honesty about untested verification (type: improve). **Shipped same day.** Patched principle #8, Testing template, Common Mistakes, and Red Flags to require unchecked `- [ ]` checklists for unverified items and forbid fabricated past-tense. GREEN-verified against /tmp/pr-45173.diff. **Open follow-up:** confirmed-similar loophole likely exists in `weekly-update-formatter` and `message-crafter` (both have confident-voice constraints) — not yet audited.
- 2026-05-26 — email-broadcast-build skill: CIO UI URL pattern in Stage 5 Step E is stale. Skill says `https://fly.customer.io/<env_id>/people/imports/<id>` (404s). Actual pattern is `https://fly.customer.io/workspaces/<env_id>/journeys/imports/<id>/setup`. Segment URL likely follows same /workspaces/<env_id>/journeys/segments/<id> pattern — verify next time. (type: improve) **Superseded:** skill migrated to `agent-os/plugins/growth/growth-engineering/email-broadcast` and rewritten; re-verify against the new layout if it recurs.
- 2026-05-26 — email-broadcast-build skill: `supabase-email-shell.html` doc-comment (line 5) contains the literal text "<!-- CONTENT_SLOT -->" describing the marker. `str.replace(marker, body)` slots the body twice — once into the comment, once into the real slot. Fix: either change the doc-comment to reference the marker by name without the literal tag (e.g. "the CONTENT_SLOT marker"), or make the marker more specific (e.g. `<!-- CONTENT_SLOT_BEGIN_HERE -->`). (type: improve) **Superseded:** same migration as above; the template now lives in the agent-os plugin.
- 2026-07-21 — weekly-review "Knowledge Freshness" query matches body `status:` lines, not just frontmatter (false positives: the project-tracking docs). Tighten to frontmatter-only. (type: improve). Why: honesty pass surfaced 2 already-done docs as stale.
- 2026-07-27 — work-sweep subagents give up too eagerly on OAuth failures and report a source as dead (type: improve). Why: the Gmail agent hit "Authentication Needed" on `helloseanoliver@gmail.com`, retried once, and returned "personal Gmail unavailable: not authenticated" per the skill's own "retry at most twice then stop" instruction. A plain retry minutes later worked with no user action. That false negative would have dropped the only two hard deadlines in the whole sweep (CCHP screening consent forms due 8/21, therapy intake docs — 2nd reminder), neither of which existed in Things or Linear. Fix: (a) raise the retry budget and add a short backoff for auth-class errors specifically, since OAuth token refresh is transient in a way that a 404 isn't; (b) when a source does fail, the synthesis must surface it as a LOUD gap in the report body — not a background-reminder line item — because a silently-missing source reads as "nothing there"; (c) prompt the agent to distinguish "token needs refresh" from "connector not authorized," which have different fixes. **Moot 2026-08-12:** the Gmail agent (Agent 6) was deleted when the Google Workspace MCP was removed.
- 2026-08-03 — work-sweep Gmail OAuth retry: the 2026-07-27 entry above is now CONFIRMED REPRODUCIBLE, raise its priority (type: improve). Why: identical failure one week later — the Gmail agent hit auth errors on all three searches for `helloseanoliver@gmail.com`, reported the source dead and asked for a browser OAuth flow, then succeeded on every query on a plain retry with no user action whatsoever. It also self-corrected in its report ("my earlier auth-failed report was premature"). Two independent occurrences means this is a deterministic property of the connector's first call in a fresh subagent, not bad luck, so the fix is not "retry more" as a nicety — it belongs in the Agent 6 prompt as a hard rule: treat the first auth error as expected, retry at least 4 times before ever reporting the source unavailable, and never surface an OAuth URL to the user until that budget is exhausted. Cost of not fixing it is asymmetric: today the retry recovered a same-day time-sensitive Lyra reschedule that existed in no other system. Also worth noting the parent-entry's point (b) held up well — surfacing the failed source LOUDLY in the summary body is what prompted the retry in the first place. **Moot 2026-08-12:** same removal — there is no Gmail agent left to fix.
- 2026-08-05 — Set up Playwright MCP extension/CDP mode so Claude can drive Sean's real Brave/Chrome session instead of an isolated profile (type: new). Recurring need: any authenticated web UI (UniFi, vendor consoles) currently requires a second login in the Playwright profile. **Likely superseded:** Claude-in-Chrome is installed and drives the real Chrome session; revisit only if a Playwright-specific workflow still needs CDP.
